<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiEditor.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiEditor"
    xmlns:mynamespace="clr-namespace:MauiEditor.View"
    Shell.FlyoutBehavior="Disabled"
    Title="Editor">

    <ShellContent
        Title="Søg data"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent
        Title="Skab data"
        ContentTemplate="{DataTemplate mynamespace:InsertDataView}"
        Route="InsertData" />

    <ShellContent
        Title="Kommune"
        ContentTemplate="{DataTemplate mynamespace:KommuneManagerView}"
        Route="Kommune" />

    <ShellContent
        Title="Opdater data"
        ContentTemplate="{DataTemplate mynamespace:UpdateDataView}"
        Route="UpdateData" />

</Shell>
